import React from 'react';
import axios from "axios";
import {Button} from "react-bootstrap";
import {useDispatch, useSelector} from "react-redux";

const Home = () => {
	return (
		<div>
			CRYPTOOOOOO!
			{/*<Button onClick={fetch} className='btn-lg'>KNOPKA</Button>*/}
		</div>
	);
};

export default Home;